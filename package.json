{
  "name": "tracker-boot-git-hooks",
  "version": "0.1.0",
  "description": "Git hooks for tracker-boot — auto-comments on stories when commits are pushed",
  "type": "module",
  "bin": {
    "tracker-boot-git-hooks": "./bin/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "os": ["darwin", "linux"],
  "license": "UNLICENSED",
  "dependencies": {
    "boxen": "^8.0.1"
  }
}
